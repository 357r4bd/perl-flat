Development Note:

./lib is the current the main line of development, but xfa-lib is purposed for the XFA integration, which will generalize symbols into objects (FLAT::RE, initially, anything later)
